#!/bin/bash

NAME="diffutils"
VERSION="3.7"

{ time \
    {
        tar -xf $NAME-$VERSION.tar.xz &&
        cd $NAME-$VERSION &&

        ./configure --prefix=/usr &&

        make &&
        make install
    }
} 2>&1 | tee /var/log/system_building/packages/$NAME.log