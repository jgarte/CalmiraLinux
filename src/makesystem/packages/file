#!/bin/bash

NAME="file"
VERSION="5.40"

{ time \
	{
		tar -xf $NAME-$VERSION.tar.gz &&
		cd $NAME-$VERSION &&

		./configure --prefix=/usr &&
		
		make &&
		make install
	}
} 2>&1 | tee /var/log/system_building/packages/$NAME.log