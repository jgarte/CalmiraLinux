#!/bin/bash

NAME="bc"
VERSION="4.0.2"

tar -xf $NAME-$VERSION.tar.xz
cd $NAME-$VERSION

CC=gcc ./configure --prefix=/usr -G -O3
make

if [ $TEST_MODE = "enable" ]; then
	make test 2>&1 |tee -a log/$NAME.log
fi

make install
